<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EJS SetUp</title>
    <!-- <style>
        body {
            background-color: rgb(144, 128, 151);
            color: rgb(155, 15, 201);
        }
    </style> -->
    <link rel="stylesheet" href="style.css">
</head>

<body>
    <h1>
        We're Learning Ejs with <%= name %>!
            <br>
    </h1>
    <ul>
        <% for (let i=0; i < iphones.length; i++) { %>
            <li>Smartphone Name =
                <%= iphones[i].title %> -Price = $<%= iphones[i].price %>
                        <img src="<%= iphones[i].image %>" alt="<%= iphones[i].title %>" />
            </li>
            <% } %>
    </ul>
</body>

</html>